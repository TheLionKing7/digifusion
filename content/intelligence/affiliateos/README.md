# AffiliateOS case study deliverables

Copy your source files here before packaging:

- `affiliateos-case-study.pdf` — full PDF case study
- `affiliateos-case-study.html` — interactive HTML edition

Then run from digifusion repo root:

```bash
node scripts/package-affos-case-study.mjs
```

Upload the generated zip to R2 (`digifusion-shop` bucket):

`Digifusion/Intelligence/affiliateos-headless-commerce-case-study.zip`

Paystack checkout: buyers use `/shop/affiliateos-headless-commerce-case-study` — not a static Payment Link.
